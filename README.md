# Anonymous code release

This repository contains code and scripts to reproduce the experiments in our submission.

## Quick start
- See `requirements.txt` (or environment instructions) and `scripts/` for running experiments.
- Results/logs are written to `outputs/`.

## Reproducibility
We provide fixed seeds and logging utilities for all runs.
